{
  "name": "wasm-intl",
  "version": "2.0.0",
  "description": "WASM ICU Intl JS Binding",
  "main": "dist/WasmIntl.js",
  "scripts": {
    "clean": "rm -rf ./dist ./node_modules",
    "protobufjs": "mkdir -p generated && cd binding && protoc --plugin=protoc-gen-ts=../node_modules/.bin/protoc-gen-ts --js_out=import_style=commonjs,binary:../generated --ts_out=../generated formatter-options.proto",
    "protobufcpp": "mkdir -p generated && cd binding && protoc -I=./ --cpp_out=../generated formatter-options.proto",
    "build": "make && tsc",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mabels/wasm-icu.git"
  },
  "keywords": [
    "WASM",
    "ICU"
  ],
  "author": "Meno Abels",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/mabels/wasm-icu/issues"
  },
  "homepage": "https://github.com/mabels/wasm-icu#readme",
  "devDependencies": {
    "@types/node": "^12.7.2",
    "ts-protoc-gen": "^0.12.0",
    "typescript": "^3.5.3",
    "wasm-libicu": "^64.2.0"
  },
  "dependencies": {
    "@types/jest": "^24.0.18",
    "jest": "^24.9.0",
    "ts-jest": "^24.0.2"
  }
}
